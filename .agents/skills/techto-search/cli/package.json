{
  "name": "techto-search-cli",
  "version": "1.0.0",
  "type": "module",
  "description": "CLI tool for searching jobs on TechTO Jobs (jobs.techto.org)",
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 30000",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
